import { StyleSheet } from 'react-native';
import { PdfView } from '../../src/AppStyles';

const styles = StyleSheet.create({
    container: PdfView.container,
    photo: PdfView.photo,
    title: PdfView.title,
    author:PdfView.author,
    pub_year:PdfView.pub_year,
    category: PdfView.category,
    btn_1: PdfView.btn_1,
    bookmark : PdfView.bookmark,
    img_align : PdfView.img_align,
    info_circle : PdfView.info_circle
  
  });

export default styles;
